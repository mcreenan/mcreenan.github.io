---
phase: 05-desktop-shell-polish
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/styles/global.css
autonomous: false
requirements: [SHELL-01, SHELL-03]

must_haves:
  truths:
    - "Desktop icons show focus rings only on keyboard navigation, not on mouse click"
    - "Tab key can reach all desktop icons and they show visible focus indicators"
    - "The retro theme's page layout and navigation are completely unchanged"
    - "Overlay windows, desktop icons, and theme switcher all function correctly across all three themes"
  artifacts:
    - path: "src/styles/global.css"
      provides: "Updated :focus-visible selectors for desktop icons"
      contains: "focus-visible"
  key_links:
    - from: "src/styles/global.css"
      to: ".pm-icon, .win95-icon"
      via: ":focus-visible pseudo-class"
      pattern: "focus-visible"
---

<objective>
Polish keyboard accessibility: update desktop icon focus selectors from :focus to :focus-visible so focus rings appear only during keyboard navigation (not mouse clicks). Verify the full overlay system and all three themes work correctly end-to-end.

Purpose: Completes keyboard accessibility (D-06) and validates the entire phase delivers correctly (SHELL-01, SHELL-03)
Output: Updated focus selectors in global.css, human-verified overlay and theme system
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-desktop-shell-polish/05-CONTEXT.md
@.planning/phases/05-desktop-shell-polish/05-RESEARCH.md
@.planning/phases/05-desktop-shell-polish/05-01-SUMMARY.md

<interfaces>
<!-- Focus selectors to update in global.css -->

Current selectors (from global.css):
```css
/* Line ~447 */
[data-theme="win31"] .pm-icon:focus {
  /* focus ring styles */
}

/* Line ~776 */
.win95-icon:focus {
  /* focus ring styles */
}

/* Line ~1103 */
.desktop-icon:focus {
  /* focus ring styles */
}
```

These should all use :focus-visible instead of :focus per D-06 and RESEARCH State of the Art section.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update :focus to :focus-visible on all desktop icon selectors</name>
  <read_first>
    - src/styles/global.css
    - .planning/phases/05-desktop-shell-polish/05-RESEARCH.md (State of the Art section and Pitfall 7)
  </read_first>
  <files>src/styles/global.css</files>
  <action>
Find and update these focus selectors in global.css from `:focus` to `:focus-visible`:

1. `[data-theme="win31"] .pm-icon:focus` (around line 447) -> `[data-theme="win31"] .pm-icon:focus-visible`
2. `.win95-icon:focus` (around line 776) -> `.win95-icon:focus-visible`
3. `.desktop-icon:focus` (around line 1103) -> `.desktop-icon:focus-visible`

Also search for any other `:focus` selectors on interactive elements that should be updated. Do NOT change `:focus-within` selectors (the menubar uses these correctly).

Per D-06: `:focus-visible` shows the focus ring only when the user navigates via keyboard (Tab key), not on mouse click. This is the modern standard and matches authentic OS behavior where clicking an icon selects it visually but doesn't show a keyboard focus ring.

Do NOT change the actual focus ring styles — only change the pseudo-class from `:focus` to `:focus-visible`.
  </action>
  <verify>
    <automated>cd /Users/mcreenan/p/mcreenan.github.io && grep -c 'pm-icon:focus[^-]' src/styles/global.css | grep -q '^0$' && grep -q 'pm-icon:focus-visible' src/styles/global.css && grep -c 'win95-icon:focus[^-]' src/styles/global.css | grep -q '^0$' && grep -q 'win95-icon:focus-visible' src/styles/global.css && grep -c 'desktop-icon:focus[^-]' src/styles/global.css | grep -q '^0$' && grep -q 'desktop-icon:focus-visible' src/styles/global.css && bun run build && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - No `.pm-icon:focus` selector remains in global.css (replaced with :focus-visible)
    - No `.win95-icon:focus` selector remains in global.css (replaced with :focus-visible)
    - No `.desktop-icon:focus` selector remains in global.css (replaced with :focus-visible)
    - All three now use :focus-visible
    - :focus-within selectors on menubar are NOT changed
    - `bun run build` succeeds
  </acceptance_criteria>
  <done>All desktop icon focus selectors updated from :focus to :focus-visible, build passes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of overlay system and all themes</name>
  <files>n/a</files>
  <action>
Human verification checkpoint. No automated action — the executor presents the verification steps below and waits for user approval.
  </action>
  <what-built>
    Complete desktop overlay system for win31 and win95 themes, with focus-visible keyboard accessibility, History API URL sync, focus trap, and all dismissal methods.
  </what-built>
  <how-to-verify>
    Run `bun run dev` and test the following:

    **Win 3.1 Theme:**
    1. Switch to win31 theme via Options > Change Theme in Program Manager menu
    2. Click the "About" icon in Program Manager — an overlay window should appear with flat gray win31 chrome containing the About page content
    3. Press Escape — overlay should close and focus should return to the About icon
    4. Click "Work" icon — overlay with Work content should appear
    5. Check URL bar — should show /work
    6. Press browser Back button — overlay should close, URL returns to /
    7. Click "Contact" icon, then click outside the overlay window — should close
    8. Tab to an icon and press Enter — overlay should open (keyboard nav)

    **Win 95 Theme:**
    9. Switch to win95 theme
    10. Click "About" desktop icon — overlay should appear with beveled 98.css chrome
    11. Click the X close button in title bar — overlay should close
    12. Verify overlay does not extend behind the taskbar at bottom
    13. Right-click desktop to open theme dialog (existing behavior still works)

    **Retro Theme:**
    14. Switch to retro theme
    15. Click About button — should navigate to /about page normally (no overlay)
    16. Verify the retro layout looks exactly as before

    **Keyboard Focus:**
    17. In win31 or win95 theme, Tab through desktop icons — focus ring should be visible
    18. Click an icon with mouse — focus ring should NOT appear (focus-visible)

    **Progressive Enhancement:**
    19. (Optional) Disable JS in DevTools, reload, click an icon — should navigate to the full page
  </how-to-verify>
  <verify>Human visual and functional verification</verify>
  <acceptance_criteria>
    - All 18 verification steps pass
    - No visual regressions in any theme
  </acceptance_criteria>
  <done>User approves all verification steps</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan only changes CSS pseudo-classes and verifies existing functionality |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Denial of Service | :focus-visible | accept | CSS-only change; no security impact |
</threat_model>

<verification>
1. `bun run build` succeeds
2. `grep -c 'icon:focus[^-]' src/styles/global.css` returns 0 (no bare :focus on icons)
3. `grep -c 'focus-visible' src/styles/global.css` returns at least 3
4. Human verification of all three themes passes
</verification>

<success_criteria>
- All desktop icon focus selectors use :focus-visible
- Full overlay system works for win31 and win95 themes
- Retro theme navigation is completely unchanged
- Focus rings visible on keyboard navigation only
- All dismissal methods work (Escape, close, backdrop, back button)
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-desktop-shell-polish/05-02-SUMMARY.md`
</output>
