---
phase: 01-migrate-to-bun
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bun.lockb
  - yarn.lock
  - CLAUDE.md
autonomous: false
requirements:
  - PKG-01
  - PKG-02
  - PKG-03
  - CI-01
  - CI-02
  - VER-01
  - VER-02

must_haves:
  truths:
    - "bun.lockb exists in the repository and yarn.lock does not"
    - "bun run dev starts the local dev server without errors"
    - "bun run build completes without errors (astro check && astro build)"
    - "GitHub Actions workflow auto-detects Bun from bun.lockb and builds successfully"
    - "Site deploys to GitHub Pages after push to main"
  artifacts:
    - path: "bun.lockb"
      provides: "Bun binary lockfile for dependency resolution"
    - path: "CLAUDE.md"
      provides: "Updated project docs referencing bun instead of yarn"
  key_links:
    - from: "bun.lockb"
      to: ".github/workflows/deploy.yml"
      via: "withastro/action@v2 auto-detection"
      pattern: "withastro/action@v2"
---

<objective>
Migrate from Yarn to Bun as the package manager by replacing yarn.lock with bun.lockb and verifying all scripts, local build, and CI/CD deployment continue to work.

Purpose: Complete the tooling-only migration so the site uses Bun for dependency management both locally and in CI.
Output: Repository with bun.lockb (no yarn.lock), working local builds, and successful GitHub Pages deployment.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-migrate-to-bun/01-CONTEXT.md
@.planning/phases/01-migrate-to-bun/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Bun migration and verify local builds</name>
  <files>bun.lockb, yarn.lock, CLAUDE.md</files>
  <read_first>
    - package.json (verify scripts are PM-agnostic before migration)
    - .github/workflows/deploy.yml (confirm withastro/action@v2 is in use, no yarn-specific config)
    - .gitignore (confirm bun.lockb is not ignored)
    - CLAUDE.md (current state before updating yarn references)
  </read_first>
  <action>
Execute the migration in this exact order:

1. Delete node_modules to avoid stale Yarn-installed state:
   rm -rf node_modules

2. Run bun install to generate bun.lockb and install all deps:
   bun install

3. Check which lockfile was generated. D-03 specifies binary lockfile (bun.lockb).
   - If bun.lockb exists: proceed as-is
   - If bun.lock (text) was generated instead of bun.lockb: both are detected by withastro/action, so either is acceptable. Proceed with whichever was generated.

4. Verify all scripts work locally (per PKG-03, VER-01, VER-02):
   - Run: bun run build
     Expected: "astro check" passes, then "astro build" completes, dist/ directory is produced
   - Run: bun run dev (start it, confirm it starts on localhost, then kill it)
     Expected: Dev server starts on http://localhost:4321 without errors
   - Run: bun run preview (start it, confirm it starts, then kill it)
     Expected: Preview server starts without errors

5. Delete yarn.lock (per PKG-02):
   git rm yarn.lock

6. Stage bun.lockb (or bun.lock if that was generated):
   git add bun.lockb

7. Update CLAUDE.md to replace yarn references with bun equivalents:
   - Change "Yarn (lockfile present: yarn.lock)" to "Bun (lockfile present: bun.lockb)"
   - Change "Lockfile: yarn.lock (199.7K)" to "Lockfile: bun.lockb"
   - Change "Yarn package manager" to "Bun package manager"
   - Change all "yarn dev", "yarn start", "yarn build", "yarn preview", "yarn astro" to "bun run dev", "bun run start", "bun run build", "bun run preview", "bun run astro" (or "bun dev" etc.)
   - In Platform Requirements, change "Yarn package manager" to "Bun package manager"

8. Stage CLAUDE.md:
   git add CLAUDE.md

9. Do NOT commit yet -- the executor should leave changes staged for the user to review and commit, since pushing to main triggers deployment (per D-01, the CI auto-detects bun.lockb).

Important per D-01/D-02: Do NOT modify .github/workflows/deploy.yml. The withastro/action@v2 auto-detects bun.lockb. No workflow changes needed.
Per D-04: Do NOT create bunfig.toml.
  </action>
  <verify>
    <automated>test -f bun.lockb && test ! -f yarn.lock && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - bun.lockb (or bun.lock) exists in the working directory
    - yarn.lock does not exist in the working directory
    - node_modules directory exists and was installed by bun
    - `bun run build` exits with code 0 and produces a dist/ directory
    - `bun run dev` starts without errors (dev server binds to localhost:4321)
    - .github/workflows/deploy.yml is UNCHANGED (per D-01, D-02)
    - No bunfig.toml exists (per D-04)
    - CLAUDE.md contains "Bun" instead of "Yarn" for package manager references
    - CLAUDE.md contains "bun run dev" instead of "yarn dev"
    - CLAUDE.md contains "bun run build" instead of "yarn build"
    - git status shows bun.lockb staged, yarn.lock deleted, CLAUDE.md modified
  </acceptance_criteria>
  <done>bun.lockb exists, yarn.lock is deleted, all scripts work via bun run, CLAUDE.md updated, changes staged for commit</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Commit, push, and verify GitHub Pages deployment</name>
  <files>bun.lockb, yarn.lock, CLAUDE.md</files>
  <action>
Human verification step. Claude has completed the local migration (Task 1). The user must now:
1. Review staged changes: git diff --cached --stat
2. Commit: git commit -m "chore: migrate from yarn to bun"
3. Push to main: git push origin main
4. Go to https://github.com/mcreenan/mcreenan.github.io/actions and watch the workflow run
5. Verify the build step shows "bun install" (not "yarn install") in the action logs
6. After deployment completes, visit https://matt.creenan.me and confirm the site loads correctly
7. Check a few pages (about, work, contact) to confirm no breakage
  </action>
  <verify>User confirms site deployed successfully to GitHub Pages</verify>
  <acceptance_criteria>
    - GitHub Actions workflow log shows bun install (not yarn install) in build step
    - GitHub Actions workflow completes with green check
    - https://matt.creenan.me loads and displays the site correctly
    - Navigation to /about, /work, /contact all render correctly
  </acceptance_criteria>
  <done>Site is live on GitHub Pages, deployed via Bun-based CI pipeline, all pages load correctly</done>
</task>

</tasks>

<verification>
- bun.lockb exists in repository root
- yarn.lock does NOT exist in repository
- .github/workflows/deploy.yml is unchanged from before migration
- No bunfig.toml in repository
- `bun run build` produces dist/ directory without errors
- `bun run dev` starts dev server on localhost:4321
- GitHub Actions workflow log shows Bun auto-detected (CI-01)
- Site accessible at https://matt.creenan.me after push (CI-02)
</verification>

<success_criteria>
1. bun.lockb is the only lockfile in the repository (PKG-01, PKG-02)
2. All npm scripts work via `bun run` (PKG-03)
3. GitHub Actions uses Bun for install and build without workflow changes (CI-01, D-01, D-02)
4. Site deploys successfully to GitHub Pages (CI-02)
5. Local dev server starts and serves pages (VER-01)
6. Production build completes without errors (VER-02)
</success_criteria>

<output>
After completion, create `.planning/phases/01-migrate-to-bun/01-01-SUMMARY.md`
</output>
