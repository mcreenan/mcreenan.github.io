---
phase: 2
slug: css-foundation-theme-infrastructure
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-21
---

# Phase 2 — UI Design Contract

> Visual and interaction contract for Phase 2: CSS Foundation and Theme Infrastructure.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — hand-rolled Astro components |
| Preset | not applicable |
| Component library | none — bespoke Astro components (Window.astro, Button.astro) |
| Icon library | Phosphor Icons (`ph ph-*`) + Font Awesome (`fa-solid`, `fa-regular`) — source: existing codebase |
| Font (body) | "Courier New", monospace — source: existing `global.css` |
| Font (title/UI labels) | 'Roboto', sans-serif — source: existing `global.css` |

shadcn gate: Not applicable. This is an Astro project — shadcn targets React/Next.js/Vite. No `components.json` present or needed.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-label gap inside desktop icons; button bottom offset |
| sm | 8px | Button internal padding (default); dialog internal padding compact |
| md | 16px | Window content padding (1rem); dialog content padding |
| lg | 24px | Nav button row gap (gap-4 = 16px, gap-6 = 24px); desktop icon gap |
| xl | 32px | Section vertical spacing (mt-8); dialog margin from viewport edge |
| 2xl | 48px | Major section breaks (mt-12 on larger screens) |
| 3xl | 64px | Page-level spacing (not used in this phase) |

Exceptions:
- Nav buttons (`.button`): 96×96px default, 72×72px at 640px, 60×60px at 380px — source: existing `global.css`
- Desktop icons: fixed 100px wide — legacy fixed size, inherited from existing codebase, out of scope for Phase 2
- Touch targets: minimum 44×44px satisfied by all existing button/icon sizes

---

## Typography

**Declared type system (2 weights, 4 sizes):**

| Role | Size | Weight | Line Height | Font |
|------|------|--------|-------------|------|
| Body / window content | 16px | 400 (regular) | 1.5 | "Courier New", monospace |
| Label / button span / dialog radio | 14px | 400 (regular) | 1.2 | "Courier New", monospace |
| Desktop icon label | 22.4px (1.4rem) | 400 (regular) | 1.2 | "Courier New", monospace |
| Display / window title | clamp(42px, 15vw, 115px) | 700 (bold) | 0.8 | 'Roboto', sans-serif |

Notes on consolidation:
- Body: existing codebase uses 1.1rem (17.6px) in some places and 16px in the dialog heading — unified to 16px for Phase 2. No visual regression expected at this rounding difference.
- Label: existing codebase uses 0.9rem (14.4px) — rounded to 14px token. Difference is 0.4px; not perceptible.
- Dialog heading ("Select Theme"): uses 16px body size at weight 700 to distinguish heading from radio labels without introducing a new size token.
- Weight 900 (black): the window title's rendered weight in the existing `global.css` is a pre-existing property outside the Phase 2 type system. Phase 2 does not add, change, or remove it. It is not a declared Phase 2 token.

**Declared weights: 400 (regular) + 700 (bold). 2 weights only.**

---

## Color

### Retro Theme Token Map

This phase tokenizes all hardcoded values into CSS custom properties under `[data-theme="retro"]`.
The 60/30/10 allocation reflects the existing visual design — nothing changes visually.

| Role | Value | Token | Usage |
|------|-------|-------|-------|
| Dominant (60%) | `#008080` | `--color-bg` | Desktop background, body fill |
| Secondary (30%) | `#c0c0c0` | `--color-window` | Window chrome, scrollbar thumb |
| Accent (10%) | `#f0f0f0` | `--color-button-bg` | Nav buttons, dialog Apply Theme button |
| Destructive | none | — | No destructive actions in this phase |

**Full retro theme token inventory** (source: RESEARCH.md §Hardcoded Values to Tokenize):

| Token | Value | Source Location |
|-------|-------|-----------------|
| `--color-bg` | `#008080` | `body` background-color |
| `--color-window` | `#c0c0c0` | `.window` background-color |
| `--color-window-content` | `#ffffff` | `.content-window` gradient start / implied |
| `--color-button-bg` | `#f0f0f0` | `.button` background-color |
| `--color-button-text` | `#000000` | `.button` color |
| `--color-button-border` | `#000000` | `.button` border color |
| `--color-link` | `#0000EE` | `.window-content a` color |
| `--color-link-hover` | `#0066FF` | `.window-content a:hover` color |
| `--color-desktop-icon` | `#ffffff` | `.desktop-icon` color |
| `--color-desktop-icon-shadow` | `rgba(0,0,0,0.7)` | `.desktop-icon` text-shadow |
| `--color-desktop-icon-highlight` | `rgba(0,0,255,0.3)` | `.desktop-icon:hover/focus` background |
| `--outline-desktop-icon` | `1px dotted white` | `.desktop-icon:focus` outline |
| `--color-desktop-label-stroke` | `#000` | `.desktop-icon span` text-shadow (4 directions) |
| `--filter-desktop-icon` | `rgba(0,0,0,0.7)` | `.desktop-icon i` filter |
| `--color-scrollbar-track` | `#f1f1f1` | `.window-content::-webkit-scrollbar-track` |
| `--border-scrollbar` | `1px solid #000` | `.window-content::-webkit-scrollbar-track` |
| `--color-scrollbar-thumb` | `#c0c0c0` | `.window-content::-webkit-scrollbar-thumb` |
| `--color-scrollbar-thumb-hover` | `#a0a0a0` | `.window-content::-webkit-scrollbar-thumb:hover` |
| `--color-title-gradient-start` | `#00FFF0` | `.window-title` gradient |
| `--color-title-gradient-end` | `#FFFFFF` | `.window-title` gradient |
| `--color-title-shadow` | `rgba(25,25,112,0.7)` | `.window-title` text-shadow |
| `--color-title-glow-1` | `rgba(0,255,240,0.4)` | `.window-title` text-shadow glow |
| `--color-title-glow-2` | `rgba(0,255,204,0.3)` | `.window-title` text-shadow glow |
| `--font-body` | `"Courier New", monospace` | `body` font-family |
| `--font-title` | `'Roboto', sans-serif` | `.os-title`, `.window-title` font-family |
| `--border-button` | `2px solid #000` | `.button` border |
| `--border-headshot` | `3px solid #000` | `.headshot-image` border |
| `--shadow-button` | `2px 2px 0 #000` | `.button` box-shadow |
| `--shadow-button-hover` | `4px 4px 0 #000` | `.button:hover` box-shadow |
| `--shadow-headshot` | `4px 4px 0 #000` | `.headshot-image` box-shadow |
| `--shadow-window` | `0 8px 12px rgba(0,0,0,0.12), 0 12px 24px rgba(0,0,0,0.1), 0 24px 32px rgba(0,0,0,0.08), 0 32px 48px rgba(0,0,0,0.06)` | `.splash-screen` box-shadow |
| `--bg-gradient-fade` | `rgba(255,255,255,0.15)` | `body` background gradient overlay |
| `--bg-pixel-light` | `rgba(255,255,255,0.08)` | `body` pixel grid light dots |
| `--bg-pixel-dark` | `rgba(0,0,0,0.05)` | `body` pixel grid dark dots |
| `--bg-splash` | `linear-gradient(145deg, #f0f0f0 0%, #e0e0e0 25%, #c8c8c8 50%, #b8b8b8 75%, #a8a8a8 100%)` | `.splash-screen` background |
| `--bg-content-window` | `linear-gradient(145deg, #ffffff 0%, #fafafa 50%, #f5f5f5 100%)` | `.content-window` background |

**NOT tokenized (retro-only by decision D-09):**
- `@keyframes borderGradient` animation colors (`#2E0854`, `#5B2D90`, `#2962FF`, `#000000`)
- `border-image-source` animated values on `.splash-screen`

**Accent reserved for:**
- Nav buttons (About, Work, Contact, Themes) background fill
- Theme dialog Apply Theme button background
- Splash screen gradient base (shared with secondary)

**Dialog backdrop:** `rgba(0,0,0,0.3)` — subtle dark overlay on `::backdrop` pseudo-element. Establishes modal affordance without obscuring the retro desktop aesthetic. Source: RESEARCH.md §Open Questions — Claude's discretion recommendation.

---

## Visual Focal Point

**Primary screen (index.astro):** The display title (`.window-title` / `.os-title`) is the primary visual anchor. It renders at `clamp(42px, 15vw, 115px)` in the cyan-to-white gradient treatment, dominates the vertical space above the nav button row, and is the first element the viewer's eye reaches. All other elements (headshot, nav buttons, desktop icon) are secondary to it in visual hierarchy.

**Content pages (about.astro, work.astro, contact.astro):** The `.window` chrome and its title bar serve as the focal point. The `ThemesIcon` and `HomeIcon` are peripheral navigation affordances and must not compete visually with the window content area.

---

## Component Inventory

### New Components (Phase 2)

#### ThemeDialog.astro

A native `<dialog>` element styled to match the retro OS window chrome. Opened via `showModal()`.

**Structure:**
```
.theme-dialog (native <dialog> element)
  .theme-dialog-content (inner wrapper, styled as .window)
    h2.theme-dialog-title ("Select Theme")
    form.theme-dialog-options (method="dialog")
      label.theme-option × 3
        input[type="radio"][name="theme"][value="retro|win31|win95"]
        span (label text: "Retro" | "Win 3.1" | "Win 95")
    button.button.theme-dialog-close[type="button"] ("Apply Theme")
```

**Visual contract:**
- Dialog chrome matches the retro `.window` style: `background: var(--bg-splash)`, `border: var(--border-button)`, `box-shadow: var(--shadow-window)`
- Dialog `::backdrop`: `rgba(0,0,0,0.3)`
- Title: `.window-title` pattern but at 16px weight 700 (not the display-scale treatment)
- Radio inputs: native `<input type="radio">` unstyled or minimal styling — browser default radio affordance is retained for accessibility
- Apply Theme button: `.button` class — same 96×96 retro button at full width inside dialog (or constrained to dialog width, auto height)
- Width: `min(320px, 90vw)` — compact OS dialog size
- Position: centered via `dialog` default `margin: auto`

**Keyboard behavior (D-07):** Fully handled by native elements — arrow keys cycle radio options, Tab moves to Apply Theme, Enter/Escape close.

**Active theme indicator (SWITCH-02):** The radio input for the current theme is `checked` on dialog open. No additional styling required.

#### ThemesIcon.astro

A button element following the `.desktop-icon` pattern from `HomeIcon.astro`. Appears on subpages (about, work, contact) positioned alongside the Home icon.

**Structure:**
```
button.desktop-icon[type="button"][onclick="window.openThemeDialog()"]
  i.ph.ph-palette
  span ("Themes")
```

**Visual contract:**
- Identical to `HomeIcon.astro` in visual treatment
- Uses `ph ph-palette` icon from Phosphor Icons (already loaded)
- Position: `position: fixed; top: 1rem;` — placed to the right of the Home icon. Offset: `left: calc(1rem + 100px + 0.5rem)` = approximately `left: 9.25rem` to avoid overlap
- No new CSS classes — reuses `.desktop-icon` entirely

### Modified Components (Phase 2)

#### index.astro — 4th nav button (D-01)

Add a `<button>` element after the Contact button, identical in structure and class to existing nav buttons. Icon: `ph ph-palette`. Label: "Themes". Trigger: `onclick="window.openThemeDialog()"`.

#### Layout.astro — data-theme attribute

Add `data-theme="retro"` to the `<html>` element as SSG fallback. The anti-flash inline script in BaseHead overrides this on client before paint.

#### BaseHead.astro — anti-flash script

Add `<script is:inline>` BEFORE the `<link>` stylesheet import. Content: synchronous localStorage read that sets `data-theme` on `document.documentElement`.

---

## Interaction Contract

### Theme Switcher Flow

```
User action                    → System response                  → Visual result
────────────────────────────────────────────────────────────────────────────────
Click "Themes" button/icon     → openThemeDialog() fires          → dialog.showModal() — dialog appears centered with backdrop
                               → Current theme radio pre-checked   → Correct option visually selected
Click radio option             → localStorage updated immediately  → data-theme attribute updated on <html> — page chrome changes instantly (D-06)
                               → No page reload                    → Transition: instantaneous attribute swap, no animation (THEME-01 is deferred)
Click Apply Theme button       → dialog.close()                   → Dialog dismissed, backdrop gone
Press Escape                   → dialog.close() (browser native)  → Same as Apply Theme
Click outside dialog area      → click event on dialog itself     → dialog.close()
Tab key in dialog              → Focus moves: radio group → Apply Theme → Native focus ring visible on focused element
Arrow keys in radio group      → Browser cycles radio options     → Active radio option checked, data-theme updates live (D-06)
```

### Theme Persistence Flow

```
Event                          → Action                           → Outcome
────────────────────────────────────────────────────────────────────────────────
Page initial load              → is:inline script reads localStorage → data-theme set before CSS applied (INFRA-03)
No saved theme                 → Default to "retro"               → Retro theme always renders correctly without localStorage
View Transitions navigation    → astro:after-swap fires           → data-theme re-applied from localStorage (INFRA-05)
New session (return visit)     → is:inline script reads localStorage → Previously selected theme applied with no flash (INFRA-04)
```

### Focus States

All interactive elements must have visible focus states:

| Element | Focus style |
|---------|-------------|
| Nav buttons `.button` | Browser default outline (`:focus-visible`) |
| Desktop icons `.desktop-icon` | `outline: 1px dotted white; outline-offset: -1px; background: rgba(0,0,255,0.3)` — source: existing `global.css` |
| ThemesIcon button | Same as `.desktop-icon` (inherits class) |
| Dialog radio inputs | Browser default focus ring |
| Dialog Apply Theme button | Browser default focus ring on `.button` |

---

## CSS Architecture Contract

### Token Scope Rule

All theme-variable values live exclusively under `[data-theme="retro"] { }` in `global.css`. The `:root` selector holds only non-theme structural values (none currently in the codebase).

### Window Class Scoping (Phase 4 Collision Prevention)

The `.window` rule must be scoped under `[data-theme="retro"]` to prevent collision with 98.css in Phase 4:

```css
/* BEFORE (current) */
.window { background-color: #c0c0c0; }

/* AFTER (Phase 2 output) */
[data-theme="retro"] .window { background-color: var(--color-window); }
```

No HTML changes needed — CSS cascade applies when `<html>` carries `data-theme="retro"`.

### Animated Gradient Border Exception (D-09)

The `@keyframes borderGradient` animation and `border-image-source` on `.splash-screen` remain as hardcoded values inside a `[data-theme="retro"]` rule block. Do not create tokens for these values.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (home nav) | "Themes" (matches About / Work / Contact peer pattern) |
| Dialog heading | "Select Theme" |
| Radio option — retro | "Retro" |
| Radio option — win31 | "Win 3.1" |
| Radio option — win95 | "Win 95" |
| Dialog close button | "Apply Theme" |
| ThemesIcon label (subpages) | "Themes" |
| Empty state | Not applicable — dialog always has 3 options |
| Error state | Not applicable — theme switching is client-side with no failure mode; if localStorage is unavailable, default "retro" silently applies |
| Destructive confirmation | None — no destructive actions in this phase |

Source: Theme names from D-12, D-13, D-14 (locked decisions). Copy for dialog elements follows OS dialog conventions from the "settings panel" intent in CONTEXT.md §Specific Ideas.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized |
| Third-party | none | not applicable |

No external component registries used in this phase. All implementation uses native HTML/CSS/browser APIs and existing Astro patterns.

---

## Visual Regression Contract

Phase success criterion 1: "The site looks identical to today after the CSS refactor — no visual regressions."

The executor must verify visual output at three breakpoints after tokenization:

| Breakpoint | What to verify |
|------------|----------------|
| Full width (> 640px) | All colors, shadows, scrollbars, title gradient, button chrome |
| 640px | Button size (72×72px), window height, `.window-content` padding and font size |
| 380px | Button size (60×60px), `.os-title` font size (2rem), reduced padding |

CSS custom property typos fail silently (fall back to `inherit` or `initial`). The token inventory table in RESEARCH.md is the authoritative checklist — tick each row after verification.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
